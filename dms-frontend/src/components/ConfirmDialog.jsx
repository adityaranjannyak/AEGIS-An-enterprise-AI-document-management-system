/* AEGIS Console: destructive actions require a deliberate, plain-language confirmation. */
import { Icon } from "./Icon.jsx";
export function ConfirmDialog({ title, message, confirmLabel = "Confirm", danger = false, busy = false, onCancel, onConfirm }) { return <div className="modal-backdrop" role="presentation"><section className="confirm-dialog" role="dialog" aria-modal="true" aria-labelledby="confirm-title"><button className="dialog-close" onClick={onCancel} aria-label="Close"><Icon name="close" /></button><div className={`dialog-icon ${danger ? "is-danger" : ""}`}><Icon name={danger ? "alert" : "lock"} /></div><h2 id="confirm-title">{title}</h2><p>{message}</p><div className="dialog-actions"><button className="btn btn-light" onClick={onCancel} disabled={busy}>Cancel</button><button className={`btn ${danger ? "btn-danger" : "btn-primary"}`} onClick={onConfirm} disabled={busy}>{busy ? "Working…" : confirmLabel}</button></div></section></div>; }
